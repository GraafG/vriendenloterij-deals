/** Single source of truth for the deployed site base URL. */
export const SITE_URL = 'https://graafg.github.io/tripper-deals';

/** Tripper.nl referral code appended to every outbound link to tripper.nl. */
export const TRIPPER_REF = 'F28D4VGY';
